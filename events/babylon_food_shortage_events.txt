namespace = food_shortage_events

food_shortage_events.1 = {
	type = country_event
	placement = scope:capital

	title = food_shortage_events.1.t
	desc = food_shortage_events.1.d
	flavor = food_shortage_events.f

	event_image = {
		video = "europenorthamerica_russian_serfs"
	}

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_skull.dds"

	duration = 3

	trigger = { # triggered by JE
	}

	immediate = {
	}

	option = { #Let them eat cake
		name = food_shortage_events.1.a
		default_option = yes

		add_modifier = {
			state_radicals_from_sol_change_mult = 0.25
		}

		add_loyalists = {
			strata = poor
			value = 0.10
		}
	}
	
	option = { #We must act now
		 name = food_shortage_events.1.b
		 default_option = no

		 trigger = {	# This event is available if the country is not collecting consumption taxes on Grain
			scope:country = {
				NOT = {
					has_consumption_tax = g:grain
				}
			}
			
			show_as_unavailable = yes
		 }	

		 add_modifier = {
			country_mandate_subsidies = g:grain
			state_welfare_payments_add = 5000
		 }
	
	
	}

}