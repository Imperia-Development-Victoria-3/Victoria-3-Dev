namespace = naval_command_check
naval_command_check.1 = {
	type = country_event
	hidden = yes
	trigger ={ 
	any_scope_state = { 
		capital = no 
		any_scope_building = {
			is_building_type = building_naval_command
			}
		} 
	}
	immediate = {
		random_scope_state = {
			limit = {
			capital = no 
			any_scope_building = {
				is_building_type = building_naval_command
				}
			}
		remove_building = building_naval_command
		}
		random_scope_state = {
			limit = {
			capital = yes 
			}
		create_building = building_naval_command
		}	
	}
}

