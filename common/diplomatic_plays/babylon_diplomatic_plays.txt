dp_babylon_unify_america = {
	war_goal = annex_country
	requires_interest_marker = no
	
	texture = "gfx/interface/icons/war_goals/annex_country.dds"
	
	selectable_in_lens = {
		OR = {
			country_has_primary_culture = cu:yankee
			country_has_primary_culture = cu:dixie
		}
	}
	
	possible = {
		NOT = { is_country_type = decentralized }
		aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }
		has_technology_researched = nationalism
		
		scope:target_country = {
			NOT = { is_subject_of = root }
			NOT = { is_country_type = decentralized }
			country_is_in_north_america = yes
			OR = {
				country_has_primary_culture = cu:yankee
				country_has_primary_culture = cu:dixie
				country_has_primary_culture = cu:afro_american
				country_has_primary_culture = cu:cajun
			}
		}
	}
	
	on_weekly_pulse = {}
	
	on_war_begins = {}

	on_war_end = {}
}

dp_babylon_colonial_war = {
	war_goal = humiliation
	texture = "gfx/interface/icons/war_goals/humiliation.dds"
	possible = {
		has_law = law_type:law_imperialism
		NOT = { is_country_type = decentralized }
		babylon_aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }
		scope:target_country = {
			NOT = { is_country_type = decentralized }
			NOT = { is_subject_of = root }
			is_country_type = recognized
		}
		custom_tooltip = {
			text = DIPLO_ACTION_RELATIONS_CORDIAL_OR_GREATER_NOT
			scope:target_country.relations:root < relations_threshold:cordial
		}				
	}
}


dp_babylon_great_war = {
	requires_interest_marker = no
	war_goal = humiliation
	texture = "gfx/interface/icons/war_goals/great_war.dds"
	possible = {
		NOT = { is_country_type = decentralized }
		babylon_aggressive_diplomatic_plays_permitted = yes
		NOT = { is_subject_of = scope:target_country }
		has_technology_researched = multilateral_alliances 
		has_global_variable = trench_works_invented_global

		scope:target_country = {
			NOT = { is_country_type = decentralized }
			NOT = { is_subject_of = root }
			has_technology_researched = multilateral_alliances 
		}
		custom_tooltip = {
			text = DIPLO_ACTION_RELATIONS_CORDIAL_OR_GREATER_NOT
			scope:target_country.relations:root < relations_threshold:cordial
		}
	}

	on_weekly_pulse = {
		every_scope_play_involved = {
			root.war = {
				if = {
					limit = {
					    has_war_support = {
					        target = prev
					        value < 0
					    }
					}

					add_war_war_support = { 
						target = prev 
						value = 10
					}
				}
			}
		}
	}

	on_war_begins = {
	}

	on_war_end = {
	}
}


# dp_jihad = {
    war_goal = conquer_state

    texture = "gfx/interface/icons/war_goals/conquer_state.dds"  # Using conquer_state texture

    possible = {
        NOT = { is_country_type = decentralized }
        aggressive_diplomatic_plays_permitted = yes
        NOT = { is_subject_of = scope:target_country }
        scope:target_country = {
            NOT = { is_country_type = decentralized }
            NOT = { is_subject_of = root }
            NOT = { religion = root_religion }  # Assuming there's a way to compare the religion of the target and the declaring country
        }
    }

    on_weekly_pulse = {
        # Potentially, add effects that simulate the ongoing conflict, such as changes in public opinion, military readiness, etc.
    }

    on_war_begins = {
        # Actions to take when war begins, such as mobilizing troops, initiating war economy measures, etc.
    }

    on_war_end = {
        # Define the consequences of winning or losing the war, such as territorial gains, reparations, changes in religious freedoms, etc.
    }
} 
