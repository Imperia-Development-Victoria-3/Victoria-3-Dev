show_unsigned_trigger = {
	trigger_if = {
		limit = {
			variable_list_size = {
				name = temp_unsigned
				value >= 1
			}
		}
		any_in_list = {
			variable = temp_unsigned
			custom_tooltip = {
				text = true_neutral_x_needs_to_sign_tt
				NOT = {
					is_target_in_variable_list = {
						name = true_neutral_applicants_consideration
						target = root
					}
				}
			}
			root = {
				remove_list_variable = {
					name = temp_unsigned
					target = prev
				}
			}
		}
	}
}
