babylon_demobilisation_decision = {
	is_shown = {
		is_diplomatic_play_committed_participant = yes
		always = no
	}
	possible = {
		is_at_war = yes
		any_scope_general = {
			is_on_front = yes
		}
	}
	when_taken = {
		every_scope_war = {
			every_scope_front = {
				every_scope_general = {
					limit = {
						is_on_front = yes
					}
					place_character_in_void = 20
				}
				every_scope_state = {
					deactivate_building = {
						building = building_barracks
					}
					activate_building = {
						building = building_barracks
					}
				}
			}
		}
		every_scope_general = {
			limit = {
				is_in_void = yes
			}
			free_character_from_void = yes
		}
	}
	ai_chance = {
		base = 0
	}
}
